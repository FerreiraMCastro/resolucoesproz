//Atividade : 
//Crie dois arquivos, um arquivo HTML com uma estrutura HTML e um h1 com o texto 
//"Conexão com um arquivo JavaScript", e um arquivo
//JS com uma função que imprima no console a frase "Conexão feita com sucesso!" e execute três vezes essa função, 
// depois link o arquivo JS no arquivo HTML e e verifique se a frase "Conexão feita com sucesso!" 
// é impressa três vezes no console.


//resolvi fazer no formato Arrow function para testar a forma de execução.

const conexao = () => {
    console.log("Conexão feita com sucesso!");
}

conexao()
conexao()
conexao()

